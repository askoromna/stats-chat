{
  "scripts": {
    "start:all": "node backend/server.js & (cd frontend && npm run start)",
    "start:front": "cd frontend && npm run start",
    "install-all": "(cd frontend && npm i) && (cd backend && npm i)"
  }
}
